<div class="h-100 row justify-content-center align-items-end">
  <div class="box p-4 col-10 glass-morph-card">
    <div class="text-center fs-1">💓</div>
    <h3 class="ps-1 pt-1 pb-2 text-center">Invite your partner!</h3>
    <%= simple_form_for(resource, as: resource_name, url: invitation_path(resource_name), html: { method: :post }) do |f| %>
      <%= f.error_notification %>
      <% resource.class.invite_key_fields.each do |field| -%>
        <div class="form-inputs">
          <%= f.input field %>
        </div>
      <% end -%>
      <div class="form-actions d-flex justify-content-end mt-4">
        <%= f.button :submit, t("devise.invitations.new.submit_button"), class:"btn btn-outline-dark w-100" %>
      </div>
    <% end %>
  </div>

  <%= image_tag 'https://res.cloudinary.com/dvgcwuo68/image/upload/v1710804956/Imagem4_hcfn7v.png', style:"max-width:30%;", class:"col-10 mb-5" %>
</div>
